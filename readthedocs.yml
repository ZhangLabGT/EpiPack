version: 2
build:
  os: ubuntu-20.04
  tools:
    python: "3.8"
  apt_packages:
    - pandoc
sphinx:
  configuration: docs/source/conf.py
python:
  install:
  - method: pip, recommonmark, sphinx==6.2.1
    path: .
    extra_requirements:
    - doc
